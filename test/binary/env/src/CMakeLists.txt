
add_executable(env main.c)

if(CMAKE_EXECUTABLE_SUFFIX)
  set(ENV_BINARY "${CMAKE_CURRENT_BINARY_DIR}/argv.${CMAKE_EXECUTABLE_SUFFIX}" PARENT_SCOPE)
else(CMAKE_EXECUTABLE_SUFFIX)
  set(ENV_BINARY "${CMAKE_CURRENT_BINARY_DIR}/argv" PARENT_SCOPE)
endif(CMAKE_EXECUTABLE_SUFFIX)
